path_classifiers:
  test:
    - "lib/*.c"
    - "tools/**/*.c"
  queries:
extraction:
  cpp:
    prepare:    # Customizable step used by all languages.
      packages:
        - clang
    configure:        # override default configure
      command:
        - echo "No configure needed yet."